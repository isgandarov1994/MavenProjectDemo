package base;

import org.openqa.selenium.WebDriver;

public class BaseTest {

    // This static driver will be created by the Hooks class
    // and used by all the other classes.
    public static WebDriver driver;
}

